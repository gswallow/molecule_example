server {
    listen 81 default;
    server_name _;
    charset utf-8;

    # max upload size
    client_max_body_size 10M;

    location / {
        uwsgi_pass  unix:///run/uwsgi/uwsgi.sock;
        include     uwsgi_params;
    }
}
